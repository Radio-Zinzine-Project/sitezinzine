<style>
.main-content .programmation-rule-form,
.main-content .programmation-rule-form *{
  box-sizing: border-box;
}

.main-content .programmation-rule-form{
  font-family: "Montserrat Alternates", sans-serif;
  max-width: 1080px;
  margin: 18px auto 28px;
  padding: 16px 16px;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 6px 18px rgba(0,0,0,0.08);
  color: #1C1C1A;
}

.main-content .programmation-rule-form h1{
  margin: 0 0 12px 0;
  text-align: center;
  color: white;
  font-weight: 900;
  font-size: 22px;
  line-height: 1.2;
  overflow-wrap: anywhere;
}

.main-content .programmation-rule-form form{
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.main-content .programmation-rule-form .grid{
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
  width: 100%;
}

.main-content .programmation-rule-form .field{
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.main-content .programmation-rule-form label{
  margin: 0 0 6px 0;
  font-weight: 800;
  color: #333;
  font-size: 14px;
}

.main-content .programmation-rule-form input[type="text"],
.main-content .programmation-rule-form input[type="date"],
.main-content .programmation-rule-form select{
  width: 100%;
  padding: 10px 12px;
  border: 1px solid #ddd;
  border-radius: 8px;
  font-size: 15px;
  background: #fff;
}

.main-content .programmation-rule-form input:focus,
.main-content .programmation-rule-form select:focus{
  outline: none;
  border-color: #E4013A;
  box-shadow: 0 0 0 3px rgba(228, 1, 58, 0.15);
}

.main-content .programmation-rule-form .field ul{
  margin: 6px 0 0 0;
  padding-left: 18px;
  color: #b30021;
  font-weight: 700;
  font-size: 13px;
}

.main-content .programmation-rule-form .rule-status{
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 12px;
  border: 1px solid #ddd;
  border-radius: 10px;
  background: #fafafa;
}

.main-content .programmation-rule-form .rule-status-label{
  font-weight: 800;
  color: #333;
  font-size: 14px;
}

.main-content .programmation-rule-form .badge{
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 6px 10px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 900;
  white-space: nowrap;
}

.main-content .programmation-rule-form .badge-active{
  background: #e7f8ec;
  color: #1d7a37;
}

.main-content .programmation-rule-form .badge-inactive{
  background: #f1f1f1;
  color: #686868;
}

.main-content .programmation-rule-form .status-help{
  margin: 0;
  color: #686868;
  font-size: 13px;
  font-weight: 600;
}

.main-content .programmation-rule-form .actions{
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.main-content .programmation-rule-form button,
.main-content .programmation-rule-form input[type="submit"],
.main-content .programmation-rule-form .btn-back{
  width: 100%;
  padding: 12px 14px;
  border: 0;
  border-radius: 26px;
  text-align: center;
  text-decoration: none;
  font-weight: 900;
  cursor: pointer;
  transition: background-color .2s ease;
}

.main-content .programmation-rule-form button,
.main-content .programmation-rule-form input[type="submit"]{
  background: #E4013A;
  color: #fff;
}

.main-content .programmation-rule-form button:hover,
.main-content .programmation-rule-form input[type="submit"]:hover{
  background: #b30021;
}

.main-content .programmation-rule-form .btn-back{
  background: #f3f3f3;
  color: #1C1C1A;
}

.main-content .programmation-rule-form .btn-back:hover{
  background: #e2e2e2;
}

@media (max-width: 960px){
  .main-content .programmation-rule-form{
    margin: 14px 12px 22px;
    padding: 14px 12px;
  }

  .main-content .programmation-rule-form .grid{
    grid-template-columns: 1fr;
  }
}

@media (max-width: 420px){
  .main-content .programmation-rule-form{
    margin: 10px 8px 16px;
    padding: 12px 10px;
    border-radius: 10px;
  }

  .main-content .programmation-rule-form h1{
    font-size: 18px;
  }

  .main-content .programmation-rule-form label{
    font-size: 13px;
  }

  .main-content .programmation-rule-form input[type="text"],
  .main-content .programmation-rule-form input[type="date"],
  .main-content .programmation-rule-form select{
    padding: 9px 10px;
    font-size: 14px;
  }

  .main-content .programmation-rule-form .rule-status{
    align-items: flex-start;
    flex-direction: column;
    padding: 9px 10px;
  }
}
</style>

<div class="programmation-rule-form">
  <h1>{{ form_title }}</h1>

  {{ form_start(form) }}

  {% if rule is defined and rule.id %}
    <div class="field">
      <label>Règle</label>
      <div>{{ rule.displayName }}</div>
    </div>

    <div class="field">
      <div class="rule-status">
        <span class="rule-status-label">État :</span>

        {% if rule.isActive %}
          <span class="badge badge-active">
            Active
          </span>
        {% else %}
          <span class="badge badge-inactive">
            Inactive
          </span>
        {% endif %}
      </div>

      {% if not rule.isActive %}
        <p class="status-help">
          Une règle inactive ne peut pas être réactivée depuis ce formulaire.
          Revenez à la liste des règles après avoir corrigé ses créneaux.
        </p>
      {% endif %}
    </div>
  {% endif %}

  <div class="field">
    {{ form_label(form.category) }}
    {{ form_widget(form.category) }}
    {{ form_errors(form.category) }}
  </div>

  <div class="grid">
    <div class="field">
      {{ form_label(form.validFrom) }}
      {{ form_widget(form.validFrom) }}
      {{ form_errors(form.validFrom) }}
    </div>

    <div class="field">
      {{ form_label(form.validUntil) }}
      {{ form_widget(form.validUntil) }}
      {{ form_errors(form.validUntil) }}
    </div>
  </div>

  {{ form_widget(form._token) }}

  <div class="actions">
    {{ form_widget(form.save) }}

    <a
      href="{{ path('admin_programmationRule_index') }}"
      class="btn-back"
    >
      Retour à la liste
    </a>
  </div>

  {{ form_end(form, { render_rest: false }) }}
</div>