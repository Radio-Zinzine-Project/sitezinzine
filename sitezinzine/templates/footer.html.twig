<style>
/* ===== Footer – base ===== */
.footer{
  font-family:"Montserrat", sans-serif;
  font-weight:400;
  background:#1C1C1A;
  color:#fff;
  font-size:14px;

  display:flex;
  flex-wrap:wrap;
  justify-content:center;
  align-items:center;

  padding:12px 14px;
  gap: 6px 12px;
  box-sizing:border-box;
}

.footer a{
  color:#fff;
  text-decoration:none;
}
.footer a:hover{
  color:#E40039;
}

.footer > div{
  margin: 4px 6px;
  white-space: nowrap;
}

/* ===== Mobile portrait : grille compacte ===== */
@media (max-width: 560px){
  .footer{
    display:grid;
    grid-template-columns: 1fr 1fr;   /* 👈 2 liens par ligne */
    gap: 6px 12px;
    justify-items:start;
    font-size:13px;
  }

  /* Bloc titre/version en pleine largeur */
  .footer > div:first-child{
    grid-column: 1 / -1;
    text-align:center;
    margin-bottom:6px;
    white-space:normal;
  }

  .footer > div{
    margin: 0;
  }
}

/* ===== Mobile paysage : encore plus condensé ===== */
@media (max-width: 900px) and (orientation: landscape){
  .footer{
    grid-template-columns: repeat(3, 1fr); /* 👈 3 colonnes en paysage */
    font-size:12.5px;
    gap: 4px 10px;
  }

  .footer > div:first-child{
    grid-column: 1 / -1;
  }
}

</style>
<div class='footer'>
    <div>
        Radio Zinzine Saint Hippolyte F-04300 Limans v1.0.110
    </div>
    <div>
        <a href="{{ path('contacts') }}">
        Contacts
    </a>
    </div>
    <div>
        <a href="{{ path('mentions') }}">
        Mentions légales
    </a>
    </div>
    <div>
        <a href="{{ path('amis') }}">
        Sites amis
    </a>
    </div>
    <div>
        <a href="{{ path('aide') }}">
        Aide à l'écoute
    </a>
    </div>
    <div>
        <a href="{{ path('zone') }}">
            Zone d'écoute
        </a>
    </div>
    <div>
    <a href="https://www.radiozinzineaix.org/" target="_blank">
                Radio Zinzine Aix</a>
    </div>
    <div>
        <a href="{{ path('admin.index') }}">
            Admin
        </a>
    </div>
</div>
